document.addEventListener('DOMContentLoaded', () => {
    // Hardcoded base Elementor JSON template
    const baseElementorTemplate = {"content":[{"id":"7715d436","settings":{"content_width":{"unit":"px","size":1320,"sizes":[]},"gap":"extended","overflow":"hidden","background_background":"classic","border_radius":{"unit":"px","top":"0","right":"0","bottom":"45","left":"45","isLinked":false},"shape_divider_bottom_width":{"unit":"%","size":100,"sizes":[]},"shape_divider_bottom_height":{"unit":"px","size":14,"sizes":[]},"shape_divider_bottom_negative":"yes","vertical_lines_class":"has-vertical-lines","lines_z_index":8,"lines_color":"#E61111","margin":{"unit":"px","top":"0","right":0,"bottom":"0","left":0,"isLinked":false},"jet_parallax_layout_list":[]},"elements":[{"id":"2db260ea","settings":{"_column_size":100,"_inline_size":null},"elements":[{"id":"2844ed5a","settings":{"space":{"unit":"vh","size":8,"sizes":[]},"space_tablet":{"unit":"vh","size":"","sizes":[]},"space_mobile":{"unit":"vh","size":"","sizes":[]}},"elements":[],"isInner":false,"widgetType":"spacer","elType":"widget"},{"id":"4bf2d607","settings":{"title":"{{client_name}}","header_size":"h1","typography_typography":"custom","typography_font_size":{"unit":"px","size":74,"sizes":[]},"typography_font_size_tablet":{"unit":"rem","size":"","sizes":[]},"typography_font_size_mobile":{"unit":"px","size":36,"sizes":[]},"typography_font_weight":"900","typography_text_transform":"uppercase"},"elements":[],"isInner":false,"widgetType":"heading","elType":"widget"},{"id":"79805aa6","settings":{"space":{"unit":"vh","size":8,"sizes":[]},"space_tablet":{"unit":"vh","size":"","sizes":[]},"space_mobile":{"unit":"vh","size":"","sizes":[]},"hide_mobile":"hidden-mobile"},"elements":[],"isInner":false,"widgetType":"spacer","elType":"widget"},{"id":"6d349e6","settings":{"text":"Divider","color":"#7E7E7E66"},"elements":[],"isInner":false,"widgetType":"divider","elType":"widget"},{"id":"42a761b9","settings":{"layout":"full_width","gap":"no","structure":"20","jet_parallax_layout_list":[]},"elements":[{"id":"38997ead","settings":{"_column_size":50,"_inline_size":null},"elements":[{"id":"7865a7a8","settings":{"title":"Description- <br><br>Website: {{website}} <br><br>\n{{service}} \u2022 {{industry}}","header_size":"h4","typography_typography":"custom","typography_font_size":{"unit":"px","size":24,"sizes":[]},"typography_font_size_tablet":{"unit":"px","size":18,"sizes":[]},"typography_font_size_mobile":{"unit":"px","size":24,"sizes":[]},"_margin_mobile":{"unit":"em","top":"0","right":"0","bottom":"1","left":"0","isLinked":false}},"elements":[],"isInner":false,"widgetType":"heading","elType":"widget"}],"isInner":true,"elType":"column"},{"id":"3e28ddc6","settings":{"_column_size":50,"_inline_size":null},"elements":[{"id":"412a6231","settings":{"editor":"<p><strong data-start=\"184\" data-end=\"196\">Overview<\/strong><br data-start=\"196\" data-end=\"199\" \/>{{overview}}<\/p>","typography_typography":"custom","typography_font_size":{"unit":"px","size":24,"sizes":[]},"typography_line_height":{"unit":"px","size":38,"sizes":[]},"typography_font_size_tablet":{"unit":"px","size":18,"sizes":[]},"typography_line_height_tablet":{"unit":"px","size":28,"sizes":[]},"typography_line_height_mobile":{"unit":"px","size":"","sizes":[]}},"elements":[],"isInner":false,"widgetType":"text-editor","elType":"widget"}],"isInner":true,"elType":"column"}],"isInner":true,"elType":"section"},{"id":"51b24ed","settings":{"space":{"unit":"vh","size":9,"sizes":[]},"space_tablet":{"unit":"vh","size":"","sizes":[]},"space_mobile":{"unit":"vh","size":"","sizes":[]}},"elements":[],"isInner":false,"widgetType":"spacer","elType":"widget"}],"isInner":false,"elType":"column"}],"isInner":false,"elType":"section"},{"id":"7657943f","settings":{"layout":"full_width","content_width":{"unit":"px","size":1320,"sizes":[]},"gap":"no","overflow":"hidden","margin":{"unit":"px","top":"0","right":0,"bottom":"0","left":0,"isLinked":false},"padding":{"unit":"px","top":"0","right":"0","bottom":"0","left":"0","isLinked":false},"jet_parallax_layout_list":[]},"elements":[{"id":"7bf20ed1","settings":{"_column_size":100,"_inline_size":null},"elements":[],"isInner":false,"elType":"column"}],"isInner":false,"elType":"section"},{"id":"39c20a58","settings":{"content_width":{"unit":"px","size":1320,"sizes":[]},"gap":"extended","jet_parallax_layout_list":[]},"elements":[{"id":"4e141ffd","settings":{"_column_size":100,"_inline_size":null},"elements":[{"id":"54e534db","settings":{"space":{"unit":"vh","size":4,"sizes":[]},"space_tablet":{"unit":"vh","size":"","sizes":[]},"space_mobile":{"unit":"vh","size":"","sizes":[]}},"elements":[],"isInner":false,"widgetType":"spacer","elType":"widget"},{"id":"56f72f39","settings":{"title":"","header_size":"h1","typography_typography":"custom","typography_font_size":{"unit":"px","size":74,"sizes":[]},"typography_font_size_tablet":{"unit":"rem","size":"","sizes":[]},"typography_font_size_mobile":{"unit":"px","size":36,"sizes":[]},"typography_font_weight":"900","typography_text_transform":"uppercase"},"elements":[],"isInner":false,"widgetType":"heading","elType":"widget"}],"isInner":false,"elType":"column"}],"isInner":false,"elType":"section"},{"id":"63e72a5e","settings":{"flex_direction":"column","jet_parallax_layout_list":[]},"elements":[{"id":"3972f1fa","settings":{"title":"The Challenge\n","header_size":"h1","typography_typography":"custom","typography_font_size":{"unit":"px","size":74,"sizes":[]},"typography_font_size_tablet":{"unit":"rem","size":"","sizes":[]},"typography_font_size_mobile":{"unit":"px","size":36,"sizes":[]},"typography_font_weight":"900","typography_text_transform":"uppercase"},"elements":[],"isInner":false,"widgetType":"heading","elType":"widget"}],"isInner":false,"elType":"container"},{"id":"552c755","settings":{"jet_parallax_layout_list":[]},"elements":[{"id":"64b52713","settings":{"editor":"{{challenge_html}}","typography_typography":"custom","typography_font_size":{"unit":"px","size":24,"sizes":[]},"typography_line_height":{"unit":"px","size":38,"sizes":[]},"typography_font_size_tablet":{"unit":"px","size":18,"sizes":[]},"typography_line_height_tablet":{"unit":"px","size":28,"sizes":[]},"typography_line_height_mobile":{"unit":"px","size":"","sizes":[]}},"elements":[],"isInner":false,"widgetType":"text-editor","elType":"widget"},{"id":"79179b3","settings":[],"elements":[],"isInner":false,"widgetType":"spacer","elType":"widget"}],"isInner":false,"elType":"container"},{"id":"6e1d9a61","settings":{"flex_direction":"column","jet_parallax_layout_list":[]},"elements":[{"id":"40f0e3aa","settings":{"title":"{{client_name}} Approach\n","header_size":"h1","typography_typography":"custom","typography_font_size":{"unit":"px","size":74,"sizes":[]},"typography_font_size_tablet":{"unit":"rem","size":"","sizes":[]},"typography_font_size_mobile":{"unit":"px","size":36,"sizes":[]},"typography_font_weight":"900","typography_text_transform":"uppercase","align":"left"},"elements":[],"isInner":false,"widgetType":"heading","elType":"widget"},{"id":"368fbd4d","settings":{"editor":"{{approach_html}}","typography_typography":"custom","typography_font_size":{"unit":"px","size":24,"sizes":[]},"typography_line_height":{"unit":"px","size":38,"sizes":[]},"typography_font_size_tablet":{"unit":"px","size":18,"sizes":[]},"typography_line_height_tablet":{"unit":"px","size":28,"sizes":[]},"typography_line_height_mobile":{"unit":"px","size":"","sizes":[]}},"elements":[],"isInner":false,"widgetType":"text-editor","elType":"widget"},{"id":"42545c82","settings":[],"elements":[],"isInner":false,"widgetType":"spacer","elType":"widget"}],"isInner":false,"elType":"container"},{"id":"7fc58ba0","settings":{"flex_direction":"column","jet_parallax_layout_list":[]},"elements":[{"id":"2c114fbf","settings":{"title":"Campaign Highlights\n\n","header_size":"h1","typography_typography":"custom","typography_font_size":{"unit":"px","size":74,"sizes":[]},"typography_font_size_tablet":{"unit":"rem","size":"","sizes":[]},"typography_font_size_mobile":{"unit":"px","size":36,"sizes":[]},"typography_font_weight":"900","typography_text_transform":"uppercase","align":"left"},"elements":[],"isInner":false,"widgetType":"heading","elType":"widget"},{"id":"7c3a4b70","settings":{"editor":"{{highlights_html_table}}","typography_typography":"custom","typography_font_size":{"unit":"px","size":24,"sizes":[]},"typography_line_height":{"unit":"px","size":38,"sizes":[]},"typography_font_size_tablet":{"unit":"px","size":18,"sizes":[]},"typography_line_height_tablet":{"unit":"px","size":28,"sizes":[]},"typography_line_height_mobile":{"unit":"px","size":"","sizes":[]}},"elements":[],"isInner":false,"widgetType":"text-editor","elType":"widget"},{"id":"6c43f5a2","settings":[],"elements":[],"isInner":false,"widgetType":"spacer","elType":"widget"}],"isInner":false,"elType":"container"},{"id":"4046d652","settings":{"flex_direction":"column","jet_parallax_layout_list":[]},"elements":[{"id":"781efd11","settings":{"title":"Timeline & Deliverables","header_size":"h1","typography_typography":"custom","typography_font_size":{"unit":"px","size":74,"sizes":[]},"typography_font_size_tablet":{"unit":"rem","size":"","sizes":[]},"typography_font_size_mobile":{"unit":"px","size":36,"sizes":[]},"typography_font_weight":"900","typography_text_transform":"uppercase","align":"left"},"elements":[],"isInner":false,"widgetType":"heading","elType":"widget"},{"id":"3eb5d547","settings":{"editor":"{{timeline_html}}","typography_typography":"custom","typography_font_size":{"unit":"px","size":24,"sizes":[]},"typography_line_height":{"unit":"px","size":38,"sizes":[]},"typography_font_size_tablet":{"unit":"px","size":18,"sizes":[]},"typography_line_height_tablet":{"unit":"px","size":28,"sizes":[]},"typography_line_height_mobile":{"unit":"px","size":"","sizes":[]}},"elements":[],"isInner":false,"widgetType":"text-editor","elType":"widget"},{"id":"31efaa46","settings":[],"elements":[],"isInner":false,"widgetType":"spacer","elType":"widget"}],"isInner":false,"elType":"container"},{"id":"133617a5","settings":{"flex_direction":"column","jet_parallax_layout_list":[]},"elements":[{"id":"70dff119","settings":{"title":"Tools & Technologies\n\n","header_size":"h1","typography_typography":"custom","typography_font_size":{"unit":"px","size":74,"sizes":[]},"typography_font_size_tablet":{"unit":"rem","size":"","sizes":[]},"typography_font_size_mobile":{"unit":"px","size":36,"sizes":[]},"typography_font_weight":"900","typography_text_transform":"uppercase","align":"left"},"elements":[],"isInner":false,"widgetType":"heading","elType":"widget"},{"id":"27ff1e84","settings":{"editor":"{{tools_html}}","typography_typography":"custom","typography_font_size":{"unit":"px","size":24,"sizes":[]},"typography_line_height":{"unit":"px","size":38,"sizes":[]},"typography_font_size_tablet":{"unit":"px","size":18,"sizes":[]},"typography_line_height_tablet":{"unit":"px","size":28,"sizes":[]},"typography_line_height_mobile":{"unit":"px","size":"","sizes":[]}},"elements":[],"isInner":false,"widgetType":"text-editor","elType":"widget"},{"id":"11321245","settings":[],"elements":[],"isInner":false,"widgetType":"spacer","elType":"widget"}],"isInner":false,"elType":"container"},{"id":"285481b3","settings":{"flex_direction":"column","jet_parallax_layout_list":[]},"elements":[{"id":"15f4562c","settings":{"title":"Client Testimonial\n","header_size":"h5","typography_typography":"custom","typography_font_size":{"unit":"px","size":35,"sizes":[]},"typography_font_size_tablet":{"unit":"rem","size":"","sizes":[]},"typography_font_size_mobile":{"unit":"px","size":36,"sizes":[]},"typography_font_weight":"900","typography_text_transform":"uppercase","align":"left"},"elements":[],"isInner":false,"widgetType":"heading","elType":"widget"},{"id":"34ea2c16","settings":{"testimonial_content":"{{testimonial_quote}}","testimonial_image":{"url":"https:\/\/theme.madsparrow.me\/most\/wp-content\/uploads\/2022\/10\/most_Master-Avatar-2.png","id":1314,"alt":"","source":"library"},"testimonial_name":"{{testimonial_author}}","testimonial_job":"{{testimonial_title_company}}","testimonial_alignment":"left","_padding":{"unit":"px","top":"20","right":"24","bottom":"20","left":"24","isLinked":false},"_background_background":"classic","_background_color":"#FFFFFF","_border_radius":{"unit":"px","top":"24","right":"24","bottom":"24","left":"24","isLinked":true},"content_typography_typography":"custom","content_typography_font_size":{"unit":"px","size":16,"sizes":[]},"name_typography_typography":"custom","name_typography_font_weight":"700","job_typography_typography":"custom","content_content_color":"#262626","name_text_color":"#262626","job_text_color":"#262626","__dynamic__":{"testimonial_image":"[elementor-tag id=\"a543277\" name=\"post-featured-image\" settings=\"%7B%7D\"]"}},"elements":[],"isInner":false,"widgetType":"testimonial","elType":"widget"}],"isInner":false,"elType":"container"}],"page_settings":[],"version":"0.4","title":"dsa","type":"page"};

    const rawCaseStudyTextElement = document.getElementById('rawCaseStudyText');
    const generateJsonBtnElement = document.getElementById('generateJsonBtn');
    const outputJsonElement = document.getElementById('outputJson');
    const downloadJsonBtnElement = document.getElementById('downloadJsonBtn');
    const errorMessagesElement = document.getElementById('errorMessages');
    const warningMessagesElement = document.getElementById('warningMessages');

    const MAX_RAW_TEXT_LENGTH = 100000; // Approx 100KB, should be generous

    let generatedJson = null;
    let clientNameForFile = 'elementor_case_study';

    generateJsonBtnElement.addEventListener('click', () => {
        clearMessages();
        outputJsonElement.value = '';
        downloadJsonBtnElement.disabled = true;
        generatedJson = null;

        const rawText = rawCaseStudyTextElement.value;

        if (!rawText.trim()) {
            showError('Please paste the raw case study text.');
            return;
        }

        try {
            // Deep clone the base JSON to avoid modifying the original object
            const templateClone = JSON.parse(JSON.stringify(baseElementorTemplate));

            const parsedData = parseRawInput(rawText);
            clientNameForFile = parsedData.client_name || 'elementor_case_study';

            generatedJson = injectDataIntoTemplate(templateClone, parsedData);

            outputJsonElement.value = JSON.stringify(generatedJson, null, 2); // Pretty print
            downloadJsonBtnElement.disabled = false;

            if (parsedData.warnings && parsedData.warnings.length > 0) {
                showWarnings(parsedData.warnings);
            }

        } catch (error) {
            showError(error.message);
            console.error('Error during JSON generation:', error);
        }
    });

    downloadJsonBtnElement.addEventListener('click', () => {
        if (!generatedJson) {
            showError('No JSON generated to download.');
            return;
        }
        const blob = new Blob([JSON.stringify(generatedJson, null, 2)], { type: 'application/json' });
        const url = URL.createObjectURL(blob);
        const a = document.createElement('a');
        a.href = url;
        const sanitizedClientName = clientNameForFile.toLowerCase().replace(/[^a-z0-9]+/g, '_').replace(/^_+|_+$/g, '');
        a.download = `${sanitizedClientName}_elementor.json`;
        document.body.appendChild(a);
        a.click();
        document.body.removeChild(a);
        URL.revokeObjectURL(url);
    });

    function showError(message) {
        errorMessagesElement.textContent = message;
        errorMessagesElement.style.display = 'block';
    }

    function showWarnings(warnings) {
        warningMessagesElement.innerHTML = '<strong>Warnings:</strong><ul>' +
            warnings.map(w => `<li>${escapeHtml(w)}</li>`).join('') +
            '</ul>';
        warningMessagesElement.style.display = 'block';
    }

    function clearMessages() {
        errorMessagesElement.textContent = '';
        errorMessagesElement.style.display = 'none';
        warningMessagesElement.innerHTML = '';
        warningMessagesElement.style.display = 'none';
    }

    function escapeHtml(unsafe) {
        if (typeof unsafe !== 'string') {
            if (unsafe === null || unsafe === undefined) return "";
            unsafe = String(unsafe);
        }
        return unsafe
             .replace(/&/g, "&amp;")
             .replace(/</g, "&lt;")
             .replace(/>/g, "&gt;")
             .replace(/"/g, "&quot;")
             .replace(/'/g, "&#039;");
    }

    function parseRawInput(rawText) {
        const warnings = [];
        let data = {
            client_name: "",
            industry: "",
            service: "",
            website: "",
            overview: "",
            key_achievements: [],
            challenge: { intro: "", points: [] },
            approach: [],
            highlights: [],
            timeline: [],
            tools: [],
            testimonial: { quote: "", author: "", title: "", company: "" },
            approach_title_override: null, // For specific approach titles like "Pixelbee's Approach"
            warnings: warnings
        };

        rawText = rawText.replace(/\r\n/g, '\n').replace(/[\u2018\u2019]/g, "'").replace(/[\u201C\u201D]/g, '"');
        const lines = rawText.split('\n').map(line => line.trim());

        const sectionStarters = {
            overview: /^Overview$/i,
            key_achievements: /^Key Achievements:?$/i,
            challenge: /^The Challenge$/i,
            approach: /^(?:Pixelbee’s|Our|The) Approach|Solution|Strategy$/i,
            highlights: /^Campaign Highlights$/i,
            timeline: /^Timeline & Deliverables$/i,
            tools: /^Tools & Technologies$/i,
            testimonial: /^Client Testimonial$/i,
        };

        let currentSection = null;
        let contentBuffer = {};
        Object.keys(sectionStarters).forEach(key => contentBuffer[key] = []);

        let lineIndex = 0;
        // First few lines for metadata
        while(lineIndex < lines.length) {
            const line = lines[lineIndex];
            if (line.startsWith('Client:')) {
                data.client_name = line.substring('Client:'.length).trim();
            } else if (line.startsWith('Industry:')) {
                data.industry = line.substring('Industry:'.length).trim();
            } else if (line.startsWith('Service:')) {
                data.service = line.substring('Service:'.length).trim();
            } else if (line.startsWith('Website:')) {
                data.website = line.substring('Website:'.length).trim();
            } else if (!data.website && line.includes('.com') && lineIndex < 5 && !Object.values(sectionStarters).some(regex => regex.test(line))) {
                 if (!line.toLowerCase().includes('overview') && !line.toLowerCase().includes('challenge')) {
                    data.website = line.trim();
                }
            } else {
                // If it's not metadata and we're past the first few lines, or it's a section starter, break to section parsing
                if (lineIndex > 4 || Object.values(sectionStarters).some(regex => regex.test(line))) {
                    break;
                }
            }
            lineIndex++;
        }

        // Section content parsing
        for (; lineIndex < lines.length; lineIndex++) {
            const line = lines[lineIndex];
            let matchedSection = false;
            for (const key in sectionStarters) {
                if (sectionStarters[key].test(line)) {
                    currentSection = key;
                    matchedSection = true;
                    if (key === 'approach' && line.toLowerCase().includes('pixelbee’s approach')) {
                        data.approach_title_override = "Pixelbee’s Approach";
                    } else if (key === 'approach' && line.toLowerCase().includes('our approach')) {
                         data.approach_title_override = "Our Approach";
                    }
                    // Capture content after colon on the title line itself
                    const titleParts = line.split(':');
                    if (titleParts.length > 1 && titleParts[1].trim() !== "") {
                        contentBuffer[currentSection].push(titleParts.slice(1).join(':').trim());
                    }
                    break;
                }
            }

            if (matchedSection) continue;

            if (currentSection && line.trim() !== "") {
                contentBuffer[currentSection].push(line.trim());
            } else if (!currentSection && line.trim() !== "") {
                // Content before any official section start (after metadata) is likely overview
                if (!contentBuffer.overview) contentBuffer.overview = [];
                 contentBuffer.overview.push(line.trim());
            }
        }

        if (contentBuffer.overview && contentBuffer.overview.length > 0) {
            data.overview = contentBuffer.overview.join('\n');
        } else {
            warnings.push("Overview section is missing or empty.");
            data.overview = "Overview not provided.";
        }

        if (contentBuffer.key_achievements && contentBuffer.key_achievements.length > 0) {
            data.key_achievements = contentBuffer.key_achievements.map(ach => ach.replace(/^[-•*]\s*/, '').trim()).filter(ach => ach);
        } else {
            const overviewStats = data.overview.match(/(\d+% increase|\d{3,}(?:,\d{3})* impressions|\d+ hours\/week saved|\d{2,}% growth)/g);
            if (overviewStats && overviewStats.length > 0) {
                data.key_achievements = overviewStats;
                warnings.push("Key Achievements section was missing, but relevant stats were inferred from the overview.");
            } else {
                warnings.push("Key Achievements section is missing.");
            }
        }

        if (contentBuffer.challenge && contentBuffer.challenge.length > 0) {
            const challengeText = contentBuffer.challenge.join('\n');
            const challengePoints = [];
            let challengeIntro = '';
            const challengeLines = challengeText.split('\n');
            let firstListItemFound = false;

            for(const cline of challengeLines) {
                const listItemMatch = cline.match(/^[-•*]\s*(.*?)(?:\s*:\s*(.*))?$/);
                if (listItemMatch) {
                    firstListItemFound = true;
                    challengePoints.push({
                        title: listItemMatch[1] ? listItemMatch[1].trim() : '',
                        description: listItemMatch[2] ? listItemMatch[2].trim() : (listItemMatch[1] && !listItemMatch[2] ? listItemMatch[1].trim() : '')
                    });
                } else if (!firstListItemFound && cline.trim()) {
                    challengeIntro += (challengeIntro ? '\n' : '') + cline.trim();
                } else if (firstListItemFound && cline.trim()) {
                    if(challengePoints.length > 0) challengePoints[challengePoints.length-1].description += '\n' + cline.trim();
                    else challengePoints.push({ title: '', description: cline.trim()});
                }
            }
            data.challenge = { intro: challengeIntro.trim(), points: challengePoints };
            if (!challengeIntro && challengePoints.length === 0 && challengeText) {
                 data.challenge = { intro: challengeText, points: [] };
            }
        } else {
            warnings.push("The Challenge section is missing.");
            data.challenge = { intro: "Challenge not specified.", points: [] };
        }

        if (contentBuffer.approach && contentBuffer.approach.length > 0) {
            data.approach = parseApproach(contentBuffer.approach, warnings);
        } else {
            const approachKeywords = ['strategy', 'solution', 'execution', 'methodology', 'how we did it', 'our process'];
            let foundFuzzyApproach = false;
            const fuzzyApproachLines = [];
            let approachStartIndex = -1;
            lines.forEach((line, idx) => {
                if (approachKeywords.some(keyword => line.toLowerCase().includes(keyword))) {
                    if(approachStartIndex === -1) approachStartIndex = idx;
                    foundFuzzyApproach = true;
                }
                if(foundFuzzyApproach && fuzzyApproachLines.length < 10 && line.trim()){ // Collect up to 10 lines
                    fuzzyApproachLines.push(line);
                }
            });
            if (foundFuzzyApproach) {
                data.approach = parseApproach(fuzzyApproachLines, warnings);
                warnings.push("Approach section was missing or not clearly identified; content was inferred based on keywords.");
            } else {
                 warnings.push("Approach section is missing.");
                 data.approach = [{ title: "Approach not specified.", items: [] }];
            }
        }

        if (contentBuffer.highlights && contentBuffer.highlights.length > 0) {
            data.highlights = parseHighlights(contentBuffer.highlights, warnings);
        } else {
            warnings.push("Campaign Highlights section is missing.");
            data.highlights = [{ metric: "Highlights", result: "Not specified" }];
        }

        if (contentBuffer.timeline && contentBuffer.timeline.length > 0) {
            data.timeline = parseTimeline(contentBuffer.timeline, warnings);
        } else {
            const timelineKeywords = ['week ', 'month ', 'phase ', 'day ', 'schedule', 'deliverable'];
            let foundFuzzyTimeline = false;
            const fuzzyTimelineLines = [];
            for(const line of lines) {
                if(timelineKeywords.some(kw => line.toLowerCase().startsWith(kw) || line.toLowerCase().includes(kw+':'))) {
                    fuzzyTimelineLines.push(line);
                    foundFuzzyTimeline = true;
                }
            }
            if(foundFuzzyTimeline){
                data.timeline = parseTimeline(fuzzyTimelineLines, warnings);
                 warnings.push("Timeline & Deliverables section was missing or not clearly identified; content was inferred based on keywords.");
            } else {
                warnings.push("Timeline & Deliverables section is missing.");
                data.timeline = [{ period: "Timeline", description: "Not specified" }];
            }
        }

        if (contentBuffer.tools && contentBuffer.tools.length > 0) {
            data.tools = parseTools(contentBuffer.tools, warnings);
        } else {
            warnings.push("Tools & Technologies section is missing.");
            data.tools = [{ category: "Tools", items: "Not specified" }];
        }

        if (contentBuffer.testimonial && contentBuffer.testimonial.length > 0) {
            data.testimonial = parseTestimonial(contentBuffer.testimonial.join('\n'), warnings);
        } else {
            const fullText = lines.join('\n');
            const quoteMatch = fullText.match(/"([^"]+)"\s*[-—–]\s*([^,]+)(?:,\s*([^,]+(?:(?:at|@|,)\s*[^,]+)?))?/i);
            if (quoteMatch) {
                let author = quoteMatch[2].trim();
                let title = "";
                let company = "";
                if (quoteMatch[3]) {
                    let attributionParts = quoteMatch[3].split(/,(?!\s*Inc\.|Ltd\.|LLC)/); // Split by comma, but not if it's part of company name like ", Inc."
                    title = attributionParts[0] ? attributionParts[0].trim() : "";
                    if (attributionParts.length > 1) {
                         company = attributionParts.slice(1).join(',').trim();
                    }
                     // Check if title itself contains "at" or "@" for company
                    const atMatch = title.match(/(.+)\s+(?:at|@)\s+(.+)/i);
                    if(atMatch){
                        title = atMatch[1].trim();
                        company = atMatch[2].trim();
                    }
                }
                data.testimonial = { quote: quoteMatch[1].trim(), author: author, title: title, company: company };
                warnings.push("Client Testimonial section was missing, but a testimonial was inferred from the text.");
            } else {
                warnings.push("Client Testimonial section is missing.");
                data.testimonial = { quote: "No testimonial available.", author: "", title: "", company:"" };
            }
        }

        if (!data.service && data.industry) {
            const parts = data.industry.split(/\s*•\s*|\s*-\s*|\s*\/\s*/);
            if (parts.length > 1) {
                data.service = parts[0].trim(); // Assume first part is service
                data.industry = parts.slice(1).join(' / ').trim();
                 warnings.push("Service was inferred from the Industry field.");
            }
        } else if (data.service && !data.industry) {
             const parts = data.service.split(/\s*•\s*|\s*-\s*|\s*\/\s*/);
            if (parts.length > 1) {
                data.industry = parts.slice(1).join(' / ').trim(); // Assume rest is industry
                data.service = parts[0].trim();
                warnings.push("Industry was inferred from the Service field.");
            }
        }

        if (!data.client_name && lines.length > 0 && lines[0].trim().length < 50 && !Object.values(sectionStarters).some(regex => regex.test(lines[0]))) {
            data.client_name = lines[0].trim();
             warnings.push("Client name was inferred from the first line of text.");
        }

        if (!data.client_name) data.client_name = "Unknown Client";
        if (!data.overview) data.overview = "Overview not provided.";
        if (!data.challenge.intro && data.challenge.points.length ===0) data.challenge.intro = "Challenge not specified.";
        if (data.approach.length === 0) data.approach.push({ title: "Approach not specified.", items: [] });
        if (data.highlights.length === 0) data.highlights.push({ metric: "Highlights", result: "Not specified" });
        if (data.timeline.length === 0) data.timeline.push({ period: "Timeline", description: "Not specified" });
        if (data.tools.length === 0) data.tools.push({ category: "Tools", items: "Not specified" });
        if (!data.testimonial.quote) data.testimonial.quote = "No testimonial available.";


        if (!data.service && !data.industry) warnings.push("Service and Industry are both missing.");
        else if (!data.service) warnings.push("Service is missing.");
        else if (!data.industry) warnings.push("Industry is missing.");

        data.warnings = warnings;
        return data;
    }

    function parseApproach(approachLines, warnings) {
        const approachSections = [];
        let currentApproach = null;
        for (const line of approachLines) {
            const trimmedLine = line.replace(/^[-•*]\s*/, '');
            const isListItem = /^[ \t]*[-•*]/.test(line) || !/[a-zA-Z]/.test(line.substring(0, Math.min(line.length,15))); // Heuristic: starts with bullet or mostly non-alpha
            const isTitleLike = trimmedLine.length > 0 && !isListItem && (trimmedLine.endsWith(':') || /^[A-Z][A-Za-z\s&',.-]+$/.test(trimmedLine) && trimmedLine.split(' ').length < 7 );


            if (isTitleLike && !isListItem) {
                if (currentApproach) approachSections.push(currentApproach);
                currentApproach = { title: trimmedLine.replace(/:$/, '').trim(), items: [] };
            } else if (trimmedLine.length > 0) {
                if (!currentApproach) { // Content before any explicit title
                    currentApproach = { title: "Details", items: [] };
                }
                const itemParts = trimmedLine.split(/:\s*(.*)/s);
                if (itemParts.length > 1 && itemParts[0].trim().length < 60 && itemParts[0].trim().length > 0) {
                    currentApproach.items.push({ label: itemParts[0].trim(), value: itemParts[1].trim() });
                } else {
                    currentApproach.items.push({ value: trimmedLine });
                }
            }
        }
        if (currentApproach) approachSections.push(currentApproach);
        if (approachSections.length === 0 && approachLines.length > 0) {
            const items = approachLines.map(l => ({value: l.replace(/^[-•*]\s*/, '').trim()})).filter(item => item.value);
            if (items.length > 0) approachSections.push({title: "Our Process", items: items});
        }
        return approachSections;
    }

    function parseHighlights(highlightLines, warnings) {
        const highlights = [];
        const contentLines = highlightLines.filter(line => !/metric\s+result/i.test(line) && line.trim() !== "");
        for (const line of contentLines) {
            const parts = line.split(/\t|\s{2,}/);
            if (parts.length >= 2 && parts[0].trim() && parts[1].trim()) {
                highlights.push({ metric: parts[0].trim(), result: parts.slice(1).join(' ').trim() });
            } else {
                const lastSpaceIndex = line.lastIndexOf(' ');
                if (lastSpaceIndex > 0) {
                    const potentialResult = line.substring(lastSpaceIndex + 1).trim();
                    const potentialMetric = line.substring(0, lastSpaceIndex).trim();
                    if (potentialMetric && potentialResult && /(\d|%|\+|\/)/.test(potentialResult)) {
                         highlights.push({ metric: potentialMetric, result: potentialResult });
                         continue;
                    }
                }
                if(line.trim()) highlights.push({ metric: line.trim(), result: "Achieved" });
            }
        }
        if (highlights.length === 0 && contentLines.length > 0) {
            warnings.push("Could not parse highlights into metric/result pairs. Using raw lines.");
            return contentLines.map(line => ({metric: line, result: "Details in description"}));
        }
        return highlights;
    }

    function parseTimeline(timelineLines, warnings) {
        const timeline = [];
        for (const line of timelineLines) {
            const cleanedLine = line.replace(/^[-•*]\s*/, '');
            if (!cleanedLine) continue;
            const parts = cleanedLine.split(/:\s*(.*)/s);
            if (parts.length > 1 && parts[0].trim().length < 50 && parts[0].trim().length > 0) {
                timeline.push({ period: parts[0].trim(), description: parts[1].trim() });
            } else if (cleanedLine.trim()){
                timeline.push({ period: "", description: cleanedLine.trim() });
            }
        }
        return timeline;
    }

    function parseTools(toolLines, warnings) {
        const tools = [];
        for (const line of toolLines) {
            const cleanedLine = line.replace(/^[-•*]\s*/, '');
            if (!cleanedLine) continue;
            const parts = cleanedLine.split(/:\s*(.*)/s);
            if (parts.length > 1 && parts[0].trim().length < 50 && parts[0].trim().length > 0) {
                tools.push({ category: parts[0].trim(), items: parts[1].trim() });
            } else if (cleanedLine.trim()) {
                tools.push({ category: "General Tools", items: cleanedLine.trim() });
            }
        }
        return tools;
    }

    function parseTestimonial(testimonialText, warnings) {
        let quote = testimonialText;
        let author = "";
        let title = "";
        let company = "";
        const attributionMatch = testimonialText.match(/(?:\n|^)\s*[-—–]\s*([^,\n]+)(?:,\s*([^,\n]+(?:(?:at|@|,)\s*[^,\n]+)?))?(?:,\s*([^,\n]+))?\s*$/i);

        if (attributionMatch) {
            quote = testimonialText.substring(0, attributionMatch.index).trim();
            author = attributionMatch[1] ? attributionMatch[1].trim() : "";

            let part2 = attributionMatch[2] ? attributionMatch[2].trim() : "";
            let part3 = attributionMatch[3] ? attributionMatch[3].trim() : "";

            if (part2) {
                const atMatch = part2.match(/(.+)\s+(?:at|@)\s+(.+)/i);
                if (atMatch) {
                    title = atMatch[1].trim();
                    company = atMatch[2].trim();
                } else {
                    title = part2; // Assume it's title if no "at/@"
                }
            }
            if(part3 && !company) { // If company wasn't parsed from part2 via "at"
                company = part3;
            } else if (part3 && company && title === part2) { // If title was all of part2, and part3 exists, it's likely company
                 company = part3; // This logic might be tricky if title itself has commas.
            }


        }
        quote = quote.replace(/^["“]|["”]$/g, '').trim();
        if (!quote && testimonialText.startsWith('"') && testimonialText.endsWith('"')) {
             quote = testimonialText.replace(/^["“]|["”]$/g, '').trim(); // If only quote and no attribution detected
        }


        if ((!quote || quote === testimonialText) && !author && testimonialText.length > 0) { // If no attribution pattern matched, assume all is quote
             quote = testimonialText.replace(/^["“]|["”]$/g, '').trim();
             warnings.push("Testimonial attribution (author/title) could not be clearly parsed. Full text used as quote.");
        } else if (quote && !author) {
            warnings.push("Testimonial quote found, but author attribution might be missing or misformatted.");
        }

        return { quote, author, title, company };
    }

    function injectDataIntoTemplate(template, data) {
        function updateElementorContent(elements) {
            if (!elements || !Array.isArray(elements)) return;

            elements.forEach(el => {
                if (el.elType === 'widget' && el.settings) {
                    if (el.widgetType === 'heading' && el.id === '4bf2d607') {
                        el.settings.title = data.client_name || "Client Name";
                    }
                    if (el.widgetType === 'heading' && el.id === '7865a7a8') {
                        let projectDetails = "Project Details<br><br>";
                        if (data.website) {
                            projectDetails += `Website: <a href="${data.website.startsWith('http') ? '' : 'https://'}${escapeHtml(data.website)}" target="_blank">${escapeHtml(data.website)}</a><br><br>`;
                        }
                        let serviceIndustry = "";
                        if(data.service) serviceIndustry += escapeHtml(data.service);
                        if(data.service && data.industry) serviceIndustry += " &bull; ";
                        if(data.industry) serviceIndustry += escapeHtml(data.industry);
                        projectDetails += serviceIndustry || "Service & Industry Not Specified";
                        el.settings.title = projectDetails;
                    }
                    if (el.widgetType === 'text-editor' && el.id === '412a6231') {
                        el.settings.editor = `<p><strong>Overview</strong><br/>${escapeHtml(data.overview).replace(/\n/g, '<br/>')}</p>`;
                        if (data.key_achievements && data.key_achievements.length > 0 && data.key_achievements.length < 6) { // Limit to 5 for brevity here
                            el.settings.editor += `<br/><p><strong>Key Achievements:</strong></p><ul>${data.key_achievements.map(ach => `<li>${escapeHtml(ach)}</li>`).join('')}</ul>`;
                        }
                    }
                    if (el.widgetType === 'heading' && el.id === '3972f1fa') {
                        el.settings.title = "The Challenge";
                    }
                    if (el.widgetType === 'text-editor' && el.id === '64b52713') {
                        let challengeHtml = "";
                        if (data.challenge.intro) {
                            challengeHtml += `<p>${escapeHtml(data.challenge.intro).replace(/\n/g, '<br/>')}</p>`;
                        }
                        if (data.challenge.points && data.challenge.points.length > 0) {
                            challengeHtml += "<ul>";
                            data.challenge.points.forEach(point => {
                                challengeHtml += `<li><p>`;
                                if (point.title) {
                                    challengeHtml += `<strong>${escapeHtml(point.title)}:</strong> `;
                                }
                                challengeHtml += `${escapeHtml(point.description).replace(/\n/g, '<br/>')}</p></li>`;
                            });
                            challengeHtml += "</ul>";
                        }
                        el.settings.editor = challengeHtml || "<p>Challenge details not provided.</p>";
                    }
                    if (el.widgetType === 'heading' && el.id === '40f0e3aa') {
                        if (data.approach_title_override) {
                            el.settings.title = data.approach_title_override;
                        } else if (data.client_name && data.client_name.toLowerCase() !== 'pixelbee' && data.client_name !== "Unknown Client") {
                            el.settings.title = `${data.client_name}'s Approach`;
                        } else {
                            el.settings.title = "Our Approach";
                        }
                    }
                    if (el.widgetType === 'text-editor' && el.id === '368fbd4d') {
                        let approachHtml = "";
                        if (data.approach && data.approach.length > 0) {
                            approachHtml += "<ul>";
                            data.approach.forEach(section => {
                                if(!section.title && !section.items) return; // Skip malformed
                                approachHtml += `<li><p><strong>${escapeHtml(section.title)}</strong></p>`;
                                if (section.items && section.items.length > 0) {
                                    approachHtml += "<ul>";
                                    section.items.forEach(item => {
                                        approachHtml += `<li><p>`;
                                        if (item.label) {
                                            approachHtml += `<strong>${escapeHtml(item.label)}:</strong> `;
                                        }
                                        approachHtml += `${escapeHtml(item.value).replace(/\n/g, '<br/>')}</p></li>`;
                                    });
                                    approachHtml += "</ul>";
                                }
                                approachHtml += "</li>";
                            });
                            approachHtml += "</ul>";
                        }
                        el.settings.editor = approachHtml || "<p>Approach details not provided.</p>";
                    }
                    if (el.widgetType === 'heading' && el.id === '2c114fbf') {
                        el.settings.title = "Campaign Highlights";
                    }
                    if (el.widgetType === 'text-editor' && el.id === '7c3a4b70') {
                        let highlightsHtml = "<table><thead><tr><th>Metric</th><th>Result</th></tr></thead><tbody>";
                        if (data.highlights && data.highlights.length > 0) {
                            data.highlights.forEach(h => {
                                highlightsHtml += `<tr><td>${escapeHtml(h.metric)}</td><td>${escapeHtml(h.result)}</td></tr>`;
                            });
                        } else {
                            highlightsHtml += `<tr><td>No highlights</td><td>N/A</td></tr>`;
                        }
                        highlightsHtml += "</tbody></table>";
                        el.settings.editor = highlightsHtml;
                    }
                    if (el.widgetType === 'heading' && el.id === '781efd11') {
                        el.settings.title = "Timeline & Deliverables";
                    }
                    if (el.widgetType === 'text-editor' && el.id === '3eb5d547') {
                        let timelineHtml = "<ul>";
                        if (data.timeline && data.timeline.length > 0) {
                            data.timeline.forEach(item => {
                                timelineHtml += `<li><p>`;
                                if (item.period) {
                                    timelineHtml += `<strong>${escapeHtml(item.period)}:</strong> `;
                                }
                                timelineHtml += `${escapeHtml(item.description).replace(/\n/g, '<br/>')}</p></li>`;
                            });
                        } else {
                            timelineHtml += "<li><p>Timeline not specified.</p></li>";
                        }
                        timelineHtml += "</ul>";
                        el.settings.editor = timelineHtml;
                    }
                    if (el.widgetType === 'heading' && el.id === '70dff119') {
                        el.settings.title = "Tools & Technologies";
                    }
                    if (el.widgetType === 'text-editor' && el.id === '27ff1e84') {
                        let toolsHtml = "<ul>";
                        if (data.tools && data.tools.length > 0) {
                            data.tools.forEach(tool => {
                                toolsHtml += `<li><p>`;
                                if (tool.category && tool.category.toLowerCase() !== "general tools") {
                                    toolsHtml += `<strong>${escapeHtml(tool.category)}:</strong> `;
                                }
                                toolsHtml += `${escapeHtml(tool.items).replace(/\n/g, '<br/>')}</p></li>`;
                            });
                        } else {
                            toolsHtml += "<li><p>Tools not specified.</p></li>";
                        }
                        toolsHtml += "</ul>";
                        el.settings.editor = toolsHtml;
                    }
                    if (el.widgetType === 'heading' && el.id === '15f4562c') {
                        el.settings.title = "Client Testimonial";
                    }
                    if (el.widgetType === 'testimonial' && el.id === '34ea2c16') {
                        el.settings.testimonial_content = data.testimonial.quote ? `“${escapeHtml(data.testimonial.quote)}”` : "No testimonial available.";
                        el.settings.testimonial_name = escapeHtml(data.testimonial.author) || "";
                        let jobTitle = escapeHtml(data.testimonial.title) || "";
                        if (data.testimonial.company) {
                            jobTitle += (jobTitle ? ", " : "") + escapeHtml(data.testimonial.company);
                        }
                        el.settings.testimonial_job = jobTitle;
                    }
                }
                if (el.elements) {
                    updateElementorContent(el.elements);
                }
            });
        }
        if (template.content) {
            updateElementorContent(template.content);
        } else {
            data.warnings.push("Warning: Elementor 'content' array not found in template. Injection incomplete.");
        }
        return template;
    }
});
console.log("script.js loaded with embedded template.");
